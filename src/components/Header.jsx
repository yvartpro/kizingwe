import { AppBar, Typography } from "@mui/material"

const Header = () => {
  return(
    <>
    <AppBar>/
      <Typography>Lycee du Saint Esprit</Typography>
    </AppBar>
    </>
  )
}

export default Header